{
  "name": "reactstream-preview",
  "version": "1.0.0",
  "description": "Preview server for ReactStream components",
  "main": "preview-server.js",
  "scripts": {
    "start": "node preview-server.js ../shared/example.js",
    "dev": "nodemon preview-server.js ../shared/example.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@babel/core": "^7.24.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
